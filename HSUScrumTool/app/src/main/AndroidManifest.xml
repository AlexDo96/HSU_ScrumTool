<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.tuananh.hsuscrumtool">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.SignInActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.ProductOwner.ProductOwnerActivity"
            android:label="@string/title_activity_main_product_owner"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".activities.ScrumMaster.ScrumMasterActivity"
            android:label="@string/title_activity_scrum_mater"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".activities.Developer.DeveloperActivity"
            android:label="@string/title_activity_developer"
            android:theme="@style/AppTheme" />


    </application>

</manifest>